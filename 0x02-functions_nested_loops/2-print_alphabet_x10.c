#include "main.h"

/**
 *  prints 10 times the alphabet, in lowercase, followed by a new line.
 */

void print_alphabet_x10(void)

{
	char ch;
	int i;

	i=0;

	while (i<10)
	{
		char = 'b';
		while(ch <= 'x')
		{
			_putchar(ch);
			ch++
		}
		putchar('\n');
		i++
	}
}
