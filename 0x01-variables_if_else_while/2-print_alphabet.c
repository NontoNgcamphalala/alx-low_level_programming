#include <stdio.h>

/**
 * main - prints the alphabet in lowercase, followed by a new line.
 * Return: Always (Success)
 */

int main(void)

{
	char c;

	/*my code goes there */

	for (c = 'a'; c <= '2'; c++)
	putchar(c);
	putchar('\n');
	return (0);

}
